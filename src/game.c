#include <stdbool.h>
#include "game.h"



bool isDead(Player me, Map map)
{

}

void processTurn(Player me, Map map)
{
    /*
     * Wait for my turn
     * checkDead && quit
     * else
     * checkDangerZone && moveOut
     * else
     * checkOrder && moveTo
     * else
     * checkClosest && moveTo && giveOrder
     */
}